import React from "react";
import styled from "styled-components";

const ryptoScreen = () => {
  return <Root>Crypto</Root>;
};

const Root = styled.section`
  min-height: 100vh;
`;

export default ryptoScreen;
